# backend

This folder is reserved for backend code (APIs, server, database).

Next steps:
- Implement a server (Express, Fastify, or similar)
- Add `index.js` or `src/` with entrypoint
- Add dependencies and scripts to `package.json`
